#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <sys/types.h>

int main() {
  char buffer[1000];
  while(1){
      printf(">>");
      fgets(buffer, sizeof(buffer), stdin);
      system(buffer);
  }
  return 0;
}
